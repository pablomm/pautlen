main {
	
    int x,y;

    printf (1 != 2);

    printf (1 != 1);

    x = 1;
    y = 2;

    printf (x != y);

    printf (x != x);

    printf (x != 1);

    printf (1 != x);

    printf (2 != x);

    printf (x != (2 + 3));

}